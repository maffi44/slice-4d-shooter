// use webrtc::{
//     peer_connection::RTCPeerConnection,
//     data_channel::RTCDataChannel;
//     data_channel::data_channel_init::RTCDataChannelInit;
//     data::RTCDataChannelInit,
//     error::RTCError,
//     message::RTCDataChannelMessage,
// };

/*
use std::net::{
    SocketAddr, SocketAddrV4, Ipv4Addr,
    UdpSocket,
};
pub struct ClientNetSystem {
    udp: UdpSocket,
    // tcp: TcpSocket,
}

impl ClientNetSystem {
    pub async fn new() -> Self {
        let tcp_sokt = TcpSocket::new();

        let addr = SocketAddr::V4(
            SocketAddrV4::new(
                Ipv4Addr::new(127, 0, 0, 1), 8000));

        ClientNetSystem {
            udp: UdpSocket::bind("0.0.0.0:8000").await.unwrap(),
            tcp: TcpSocket::bind(addr).aw,

        }
    }

    pub fn connect_to_server(&mut self) {

    }
}

pub struct ServerNetSystem {
    udp: UdpSocket,
    tcp: TcpSocket,
}

impl ServerNetSystem {
    pub async fn new() -> Self {
        ServerNetSystem {
            udp: UdpSocket::bind("0.0.0.0:8000").await.unwrap(),
            tcp: TcpSocket::,

        }
    }

    pub fn connect_to_server(&mut self) {

    }
} */